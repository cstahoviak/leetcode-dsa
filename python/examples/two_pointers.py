"""
Examples the implement the "Two Pointers" algorithm.
"""
from typing import Iterable

def check_for_target(values: Iterable[int], target: int):
  """
  An implementation of the "Two Sum" algorithm.

  Args:
    values: A container of sorted, unique integers.
    target: The target sum.

  Returns:
    True if there is at least one pair of integers in 'values' that sum to the
    target sum, otherwise False.
  """
  pass
